        // int sum = Integer.MIN_VALUE;
        // for (int i = 0; i <= arr.length - k; i++) {
        //     int temp = 0;
        //     for (int j = i; j < i + k; j++) {
        //         temp += arr[j];
        //         sum = Math.max(sum, temp);
        //     }
        // }